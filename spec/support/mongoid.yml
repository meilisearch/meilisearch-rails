test:
  clients:
    default:
      database: "<%= "#{ENV['MONGODB_DATABASE'] || '_ms_rails_test'}_#{SecureRandom.hex(8)}" %>"
      hosts:
        - "<%= ENV['MONGODB_HOST'] || 'localhost:27017' %>"
      options:
        read:
          mode: :primary
        max_pool_size: 1
